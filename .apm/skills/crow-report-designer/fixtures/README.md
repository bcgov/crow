# Synthetic fixtures

These fixtures are public, synthetic examples for reviewing handoff and
session contracts. They contain no project evidence and are not claims that a
live build or provider query ran.

- `handoff-valid.yml` is a minimal signed-off handoff.
- `handoff-invalid-missing-artifact.yml` demonstrates a rejected handoff.
- `session-paused.md` demonstrates a resumable paused interview.
